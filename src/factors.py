"""
Factor exposure via public Fama-French factor sets (stand-in for
proprietary Barra factors).

TODO: pull factor returns from Ken French's Data Library (via
pandas-datareader), regress each holding's excess return against the
factors to estimate market/size/value/momentum betas.
"""
